## 介绍
Vite 是一个现代化的前端构建工具(脚手架)；加快、规范vue开发
## 项目结构
```
big-event-frontend/
├── index.html              # HTML 入口文件                [T]
├── package.json            # 项目配置和依赖
├── vite.config.js          # Vite 配置文件                [T]
├── public/                 # 静态资源目录
├── src/                    # 源代码目录
│   ├── main.js             # 应用入口文件                 [T]
│   ├── App.vue             # 根组件                       [T]
│   ├── assets/             # 样式和资源文件
│   │   └── main.scss       # 全局样式
│   ├── api/                # API 接口封装            
│   │   ├── user.js         # 用户相关 API
│   │   └── article.js      # 文章相关 API
│   ├── components/         # 组件目录
│   ├── router/             # 路由配置
│   │   └── index.js        # 路由定义                     [T]
│   ├── stores/             # Pinia 状态管理
│   │   ├── token.js        # Token 状态管理               [T]
│   │   └── userInfo.js     # 用户信息状态管理             [T]
│   ├── utils/              # 工具函数
│   │   └── request.js      # Axios 请求封装               [T]
│   └── views/              # 页面组件
│       ├── Layout.vue      # 主布局组件                   [T]
│       ├── Login.vue       # 登录页面                     [T]
│       ├── article/        # 文章相关页面
│       │   ├── ArticleCategory.vue    # 文章分类管理
│       │   └── ArticleManage.vue      # 文章管理
│       └── user/           # 用户相关页面
│           ├── UserAvatar.vue         # 用户头像设置
│           ├── UserInfo.vue           # 用户信息管理
│           └── UserResetPassword.vue  # 重置密码
```


## 启动
Vite脚手架自动构建并启动我们的项目，并打包我们的代码，类似Maven
```shell
npm create vue@latest
cd <your-project-name> 
npm install 
npm run dev // 启动开发服务器
npm run build // 构建生产版本
npm run preview// 预览生产构建结果
```

## Vite配置文件
vite.config.js配置文件
#question 
>[!notes]- 别名复杂版本写法
> alias: { '@': fileURLToPath(new URL('./src', import.meta.url)) }
```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],                 // 引入 @vitejs/plugin-vue 插件，让 Vite 能够识别和编译 .vue 单文件组件
  resolve: {
    alias: {
      '@': './src'                  // 设置 @ 别名指向 src 目录，可以用 `@/xxx/A.vue` 代替 `../../xxx/A.vue`
    }
  },
  server: {  // Vite 开发服务器会代理转发到后端服务器
    proxy: {
      '/api': {
        target: 'http://localhost:8080',              // 代理到后端服务器所在URL源
        changeOrigin: true,                           // 是否修改URL源，修改请求头中的 origin 字段
        rewrite: (path) => path.replace(/^\/api/, '') // 将路径中的 /api 前缀去掉
      }
    }
  }
})
```
server.proxy: **代理规则**
- 当前端请求 `/api/user/login` 时 
```txt
浏览器请求: http://localhost:5173/api/user/login
         ↓
Vite 代理转发: http://localhost:8080/user/login
```

## main.js文件
应用入口文件，负责：
- 导入全局样式
- 创建 Vue 应用实例
- 配置 Element Plus UI 库（中文本地化）
- 配置 Vue Router
- 配置 Pinia 状态管理（带持久化插件）
- 挂载应用到 DOM
- .....
```typescript
import './assets/main.scss'

import { createApp } from 'vue'
import router from '@/router'
import App from './App.vue'

import {createPinia} from 'pinia'
import { createPersistedState } from 'pinia-persistedstate-plugin'

import 'element-plus/dist/index.css'
import ElementPlus from 'element-plus'
import locale from 'element-plus/dist/locale/zh-cn.js'

const app = createApp(App);                 // 创建app实例
const pinia = createPinia();                // 实例化状态管理组件
const persist = createPersistedState();     // 实例化持久组件
pinia.use(persist)                          // 状态管理开启持久化
app.use(pinia)                              // app开启状态管理
app.use(router)                             // app开启路由功能
app.use(ElementPlus,{locale});              // app使用ElementPlus组件库
app.mount('#app')                           // 挂载app
```

## index.html文件
```vue
<!-- index.html -->
  <body>
    <div id="app"></div>          // main.js挂载app的地方
    <script type="module" src="/src/main.ts"></script>
  </body
```

## .Vue文件结构
```vue
<template>
	// 定义结构(html)
</template>

<script>
	// 定义行为(js/ts)
</script>

<style scoped>
	// 定义样式(css)
</style
```

## App.Vue根组件
- **文件位置**：`src/App.vue`
- **功能**：应用的根组件，负责渲染当前路由对应的组件。
#question 一般负责渲染当前路由组件？

**代码结构**：
```vue
<script setup>
// 这里可以写组件逻辑，但这个文件很简单
</script>

<template>
  <router-view></router-view>  <!-- 显示当前路由组件 -->
</template>

<style scoped>
/* 组件样式 */
</style>
```

**解释**：`<router-view>` 是 Vue Router 的内置组件，用于显示匹配当前路由的组件，非常简单，因为主要逻辑在子组件中

## 总结
- `vite.config.js`：配置vite脚手架，api，别名，代理转发等
- `index.html`：是**项目入口**，项目最外层，单html文件；vite解析index.html `<script type="module" src="xxx">`指向**main.ts**文件
- `main.js`：导入并开启路由、状态管理、第三方组件库等等组件，最主要的就是管理全局组件，使用全局组件
- `Vue3`：通过createApp 函数创建应用实例




